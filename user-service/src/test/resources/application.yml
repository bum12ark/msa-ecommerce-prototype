spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

# http response utf-8 설정 (MockMvc 사용 시 한글 깨짐 이슈)
server.servlet.encoding.force-response: true